using UnityEngine;

// Defines the possible firing types for weapons
// We can add more, like throwing or explosives later on
public enum FireType
{
    Automatic,
    SingleFire,
    Shotgun
}

// Defines the possible reload types for weapons
public enum ReloadType
{
    Magazine,
    SingleShot,
    Tube
}
